{
  "name": "gridsome",
  "private": true,
  "scripts": {
    "build": "yarn functions:build && gridsome build",
    "develop": "gridsome develop",
    "explore": "gridsome explore",
    "functions:build": "netlify-lambda build server/functions-src",
    "functions:serve": "netlify-lambda serve server/functions-src"
  },
  "dependencies": {
    "gridsome": "^0.5.0"
  },
  "devDependencies": {
    "axios": "^0.18.0",
    "mongoose": "^5.5.5",
    "netlify-lambda": "^1.4.7"
  }
}
